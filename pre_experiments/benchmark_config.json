{
  "seeds": [3407, 3408, 3409],
  "metric_scale": "normalized",
  "artifact_policy": "metrics",
  "training_defaults": {
    "loss": "MSE",
    "target_metric": "MSE",
    "learning_rate": 0.0002,
    "weight_decay": 0.0005,
    "early_stopping_patience": 10
  },
  "models": {
    "Crossformer": {
      "epochs": 80,
      "batch_size": 32,
      "source": "Project benchmark budget for Crossformer"
    },
    "PatchTST": {
      "epochs": 100,
      "batch_size": 64,
      "source": "Project benchmark budget for PatchTST"
    },
    "TimesNet": {
      "epochs": 80,
      "batch_size": 32,
      "source": "Project benchmark budget for TimesNet"
    },
    "iTransformer": {
      "epochs": 80,
      "batch_size": 32,
      "source": "Project benchmark budget for iTransformer"
    },
    "TimeMixer": {
      "epochs": 80,
      "batch_size": 32,
      "source": "Project benchmark budget for TimeMixer"
    },
    "WPMixer": {
      "epochs": 80,
      "batch_size": 64,
      "source": "Project benchmark budget for WPMixer"
    },
    "TimeFilter": {
      "epochs": 80,
      "batch_size": 32,
      "source": "Project benchmark budget for TimeFilter"
    },
    "MultiPatchFormer": {
      "epochs": 80,
      "batch_size": 32,
      "source": "Project benchmark budget for MultiPatchFormer"
    }
  },
  "dataset_batch_caps": {
    "Electricity": 16,
    "Traffic": 8
  }
}
